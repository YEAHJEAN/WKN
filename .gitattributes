# 모든 텍스트 파일에 대해 LF 사용
* text=auto eol=lf

# 특정 파일 형식에 대해 CRLF 사용
*.bat text eol=crlf
*.ps1 text eol=crlf

# 이미 바이너리 파일로 취급되는 파일
*.jpg binary
*.png binary
*.gif binary

# .gitattributes와 .gitignore 파일에 대해 LF 사용
*.gitattributes text eol=lf
*.gitignore text eol=lf

# 특정 파일 형식에 대해 LF 사용 (Windows와 Unix 환경 모두에서 일관성 유지)
*.yml text eol=lf
*.js text eol=lf
*.css text eol=lf
*.html text eol=lf
*.md text eol=lf
*.json text eol=lf